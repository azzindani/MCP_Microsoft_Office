[project]
name = "office-mcp"
version = "0.1.1"
description = "MCP servers for Microsoft Office file formats"
requires-python = "==3.14.*"
readme = "README.md"
license = { text = "MIT" }

[tool.uv.workspace]
members = [
    "shared",
    "servers/docx_basic",
    "servers/docx_tables",
    "servers/docx_layout",
    "servers/docx_new",
    "servers/xlsx_basic",
    "servers/xlsx_formulas",
    "servers/xlsx_charts",
    "servers/xlsx_new",
    "servers/pptx_basic",
    "servers/pptx_design",
    "servers/pptx_new",
]

[tool.uv.sources]
shared = { workspace = true }

[tool.uv]
required-version = ">=0.5"

[tool.ruff]
line-length = 120
target-version = "py314"
exclude = ["*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = [
    "shared",
    "docx_basic", "docx_tables", "docx_layout", "docx_new",
    "xlsx_basic", "xlsx_formulas", "xlsx_charts", "xlsx_new",
    "pptx_basic", "pptx_design", "pptx_new",
]

[tool.pyright]
pythonVersion = "3.14"
typeCheckingMode = "basic"
reportMissingImports = true
reportMissingTypeStubs = false
extraPaths = ["shared"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.9",
]
